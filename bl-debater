#!/usr/bin/env python3
"""Wrapper script for bl-debater CLI."""
import sys
from pathlib import Path

# Add src to path
src_dir = Path(__file__).parent / "src"
sys.path.insert(0, str(src_dir))

from bl_debater.cli import main

if __name__ == "__main__":
    main()
